@namespace SmartParkingSystem.Maui.Components.Pages.Workspace.Parts
@using SmartParkingSystem.Domain.Models.Localization
@using SmartParkingSystem.Maui.Components.Pages.Connection.Parts
@inherits WorkspaceSettingsViewBase

<section class="grid gap-4 lg:grid-cols-[0.95fr_1.05fr]">
    <div class="grid gap-4">
        <article class="@CurrentDeviceClass" style="@CurrentDeviceStyle">
            <div class="mb-8 flex items-center justify-center gap-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-md bg-white/85 text-calm-700">
                    <i data-lucide="monitor-smartphone"></i>
                </div>

                <div>
                    <p class="text-base font-semibold text-calm-900">@Texts.CurrentDeviceTitle</p>
                </div>
            </div>

            <div class="grid gap-3">
                @foreach (var item in DeviceItems)
                {
                    <div class="rounded-md bg-white/85 px-4 py-4">
                        <p class="text-sm font-semibold text-calm-500">@item.Label</p>
                        <p class="text-base font-semibold text-calm-900">@item.Value</p>
                    </div>
                }
            </div>
        </article>

        <article class="@InterfaceLanguageClass" style="@InterfaceLanguageStyle">
            <div class="mb-8 flex items-center justify-center gap-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-md bg-brand-100/80 text-calm-700">
                    <i data-lucide="languages"></i>
                </div>

                <div>
                    <p class="text-base font-semibold text-calm-900">@Texts.InterfaceLanguageTitle</p>
                </div>
            </div>

            <div class="grid gap-5">
                <div class="grid gap-3">
                    <p class="text-center text-sm font-semibold text-calm-500">@Texts.LocalizationLabel</p>
                    <ConnectionLanguageSwitch EnglishButtonClass="@GetLanguageButtonClass(AppLanguage.English)"
                                              UkrainianButtonClass="@GetLanguageButtonClass(AppLanguage.Ukrainian)"
                                              OnLanguageSelected="SetLanguageAsync"/>
                </div>
            </div>
        </article>
    </div>

    <div class="grid gap-4">
        <article class="@EnvironmentClass" style="@EnvironmentStyle">
            <div class="mb-8 flex items-center justify-center gap-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-md bg-white/85 text-mint-700">
                    <i data-lucide="badge-info"></i>
                </div>

                <div>
                    <p class="text-base font-semibold text-calm-900">@Texts.EnvironmentTitle</p>
                </div>
            </div>

            <div class="grid gap-3">
                @foreach (var item in EnvironmentItems)
                {
                    <div class="rounded-md bg-white/85 px-4 py-4">
                        <p class="text-sm font-semibold text-calm-500">@item.Label</p>
                        <p class="text-base font-semibold text-calm-900">@item.Value</p>
                    </div>
                }
            </div>
        </article>

        <article class="@BackendClass" style="@BackendStyle">
            <div class="mb-8 flex items-center justify-center gap-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-md bg-brand-100/80 text-calm-700">
                    <i data-lucide="server"></i>
                </div>

                <div>
                    <p class="text-base font-semibold text-calm-900">@Texts.BackendTitle</p>
                </div>
            </div>

            <div class="grid gap-3">
                <div class="rounded-md bg-white px-4 py-4">
                    <div class="flex items-center justify-between gap-3 rounded-md bg-calm-50 px-4 py-4">
                        <div>
                            <p class="text-sm font-semibold text-calm-900">@Texts.BackendSyncEnabledLabel</p>
                        </div>

                        <button class="@GetBackendToggleButtonClass(BackendSyncEnabled)"
                                type="button"
                                @onclick="ToggleBackendSyncAsync">
                            <span>@(BackendSyncEnabled ? Texts.EnabledLabel : Texts.DisabledLabel)</span>
                        </button>
                    </div>
                </div>

                <label class="grid gap-2">
                    <span class="text-sm font-semibold text-calm-500">@Texts.BackendBaseUrlLabel</span>
                    <input class="min-h-12 rounded-md bg-brand-100/70 px-4 py-3 text-sm font-semibold text-calm-900 outline-none transition-all duration-500 ease-out"
                           value="@BackendBaseUrl"
                           placeholder="@Texts.BackendBaseUrlPlaceholder"
                           @onchange="OnBackendBaseUrlChanged"/>
                </label>
            </div>
        </article>
    </div>
</section>
